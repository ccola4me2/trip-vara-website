{
  "name": "trip-vara-portal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Trip Vara advisor portal: a travel-advisor CRM front end over GoHighLevel, on Cloudflare Workers + D1.",
  "scripts": {
    "check:all": "npm run check:migrations && npm run check:wiring && npm run check:payments && npm run check:writes && npm run check:forms && npm run check:email && npm run check:routes && npm run check:columns && npm run check:contracts && npm run check:smoke",
    "check:columns": "node scripts/check-columns.mjs",
    "check:contracts": "node scripts/check-contracts.mjs",
    "check:scope": "node scripts/check-scope.mjs",
    "check:smoke": "node scripts/smoke.mjs",
    "db:migrate": "wrangler d1 migrations apply trip-vara --remote",
    "db:migrate:local": "wrangler d1 migrations apply trip-vara --local",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "precheck:columns": "node scripts/check-scope.mjs",
    "check:routes": "node scripts/check-routes.mjs",
    "check:migrations": "node scripts/check-migrations.mjs",
    "check:payments": "node scripts/check-payments.mjs",
    "check:wiring": "node scripts/check-wiring.mjs",
    "check:writes": "node scripts/check-writes.mjs",
    "check:forms": "node scripts/check-forms.mjs",
    "check:email": "node scripts/check-email.mjs"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
